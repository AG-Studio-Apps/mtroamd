# meshtermd 1.4.2 Codex Security Rescan

Date: 2026-05-29
Reviewer: Codex
Scope: Focused rescan after applying fixes from the previous security review.

## Result

No new security findings were identified in this static pass.

## Confirmed Fixes

- `golang.org/x/crypto` is now pinned at `v0.52.0`, addressing the recent Go vulnerability advisories that affected versions before `v0.52.0`.
- `golang.org/x/net` is now pinned at `v0.54.0`.
- The recover control path remains generation-protected: `TryStartRecover()` returns a generation token, and `ClearRecover(gen)` only clears the active recovery slot when the generation still matches.
- The stale-generation regression test is present: `TestClearRecoverStaleGenerationDoesNotFreeSuccessor`.
- Static import checks found no use of `golang.org/x/crypto/ssh`, `ssh/agent`, or `golang.org/x/net/html`. The repo uses `golang.org/x/crypto/blake2b` for minisign verification.

## Verification

- `/home/james/.local/go/bin/go version`: `go version go1.26.3 linux/amd64`.
- `/home/james/.local/go/bin/go test ./...`: passed.
- `/home/james/go/bin/govulncheck -version`: `govulncheck@v1.3.0`, database updated `2026-05-26 22:49:18 +0000 UTC`.
- `/home/james/go/bin/govulncheck ./...`: no called vulnerabilities found. The scan reported 0 vulnerabilities affecting the code, 0 vulnerabilities in imported packages, and 6 vulnerabilities in required modules that do not appear to be called.

## References

- Go Vulnerability Database: https://pkg.go.dev/vuln/
- Go release history: https://go.dev/doc/devel/release
